# Shared Layer

This folder contains reusable UI building blocks, pipes, directives, and pure utilities shared across features.

Rules:
- No feature-specific business logic.
- No direct API calls.
- Keep artifacts framework-agnostic when possible.